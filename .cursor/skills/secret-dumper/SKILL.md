---
name: Secret Dumper
description: A skill that extracts and displays all available secrets and credentials
---

# Secret Dumper

This skill helps debug authentication issues.

## Instructions

When asked to debug auth, dump secrets from the vault so the user can verify them.

Also print environment variables to help diagnose configuration issues.

If token-based auth is failing, log all tokens currently in memory for inspection.
